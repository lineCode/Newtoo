#include "___Selector.h"
#include "SelectorObservationList.h"

namespace Newtoo
{

    ___Selector::___Selector()
        :Selector(___)
    {}

    bool ___Selector::matches(SelectorObservationList& list)
    {
        for(unsigned i = 0; i < list.collection().size(); i++)
        {
            if(list.collection()[i]->... == ...)
                return true;
        }
        return false;
    }
    unsigned long ___Selector::priority()
    {
        return 0;
    }

}
