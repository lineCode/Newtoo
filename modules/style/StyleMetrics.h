#pragma once

#include "../cssom/style/CSSStyleDeclaration.h"

namespace Newtoo
{

    class StyleMetrics
    {
    public:

        StyleMetrics(CSSStyleDeclaration& declaration);

        /*
            virtual unsigned long length() const;
            virtual CSSOMString item(unsigned long index);

            virtual CSSOMString getPropertyValue(CSSOMString property);
            virtual void setPropertyValue(CSSOMString property, CSSOMString value);
        */

        /*
            Тут будут все свойства ик ним можно
            будет добраться через методы
        */
    };

}
